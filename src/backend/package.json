{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "go run cmd/server/main.go",
    "build": "go build -o bin/server cmd/server/main.go",
    "test": "go test ./...",
    "test:coverage": "go test -cover ./...",
    "test:verbose": "go test -v ./...",
    "lint": "command -v golangci-lint >/dev/null 2>&1 && golangci-lint run ./... || (echo 'golangci-lint not installed, skipping' && go vet ./...)",
    "lint:fix": "command -v golangci-lint >/dev/null 2>&1 && golangci-lint run --fix ./... || echo 'golangci-lint not installed, skipping'",
    "clean": "rm -rf bin"
  }
}
