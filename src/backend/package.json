{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "go run cmd/server/main.go",
    "build": "go build -o bin/server cmd/server/main.go",
    "test": "go test ./...",
    "lint": "go vet ./...",
    "clean": "rm -rf bin"
  }
}
