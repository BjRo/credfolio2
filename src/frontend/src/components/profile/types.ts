import type {
  ProfileEducation,
  ProfileExperience,
  ProfileSkill,
  ResumeExtractedData,
} from "@/graphql/generated/graphql";

export type ProfileData = ResumeExtractedData;
export type { ProfileExperience, ProfileEducation, ProfileSkill };
